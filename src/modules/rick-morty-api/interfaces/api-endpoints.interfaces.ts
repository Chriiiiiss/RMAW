interface TApiEndpoints {
  CHARACTERS: string;
  LOCATIONS: string;
  EPISODES: string;
}

export const API_ENDPOINTS: TApiEndpoints = {
  CHARACTERS: 'api/character',
  LOCATIONS: 'api/location',
  EPISODES: 'api/episode',
};
