interface IApiEndpoints {
  CHARACTERS: string;
  LOCATIONS: string;
  EPISODES: string;
}

export const API_ENDPOINTS: IApiEndpoints = {
  CHARACTERS: 'api/character',
  LOCATIONS: 'api/location',
  EPISODES: 'api/episode',
};
